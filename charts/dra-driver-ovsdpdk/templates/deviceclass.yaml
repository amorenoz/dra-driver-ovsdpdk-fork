apiVersion: resource.k8s.io/v1
kind: DeviceClass
metadata:
  name: ovsdpdk
  labels:
    {{- include "chart.labels" . | nindent 4 }}
spec:
  selectors:
    - cel:
        expression: 'device.driver == "ovsdpdk.k8snetworkplumbingwg.io"'
